# Changelog

Generated from the commit messages, which follow the Angular convention:

    python3 tools/changelog.py > CHANGELOG.md

## New

- **install** : fetch the sherpa-onnx wheel that talks to the card (`c8093d2`)
- **graph** : an index of what the tool knows, and a preparation that opens on it (`5310fca`)
- **dictation** : one click to speak, and the silence ends the sentence (`92c2b63`)
- **window** : preparation has its own tab, and the threads can be cleared (`f0d6ff4`)
- **tongue** : the tool speaks the language of the machine it runs on (`8ec8303`)
- **window** : the Conversation tab prepares the next meeting, out loud (`c3d845e`)
- **prepare** : answering somebody who is preparing a meeting (`46526ff`)
- **dictation** : a microphone held down, and a sound saying she heard (`bf89543`)
- **preparation** : a meeting prepared before it is held, and taken by it (`1c88572`)
- **api** : an HTTP door, so a site can drive the tool (`ffa2b41`)
- **memory** : what earlier meetings left now reaches the next one (`7182d80`)
- **assistant** : a discreet sound when she looks something up (`e4dcc4b`)
- **window-proof** : photograph the window on X11 too, not only on macOS (`8b0a515`)
- **tests** : the whole chain proves itself on Linux too (`736bdb6`)
- **minutes** : the names given during the meeting reach the minutes (`5a1e248`)
- **assistant** : give it the glossary of the setting, not only the writer (`5113e1f`)
- **diagnostic** : look at the models and the voice bank (`5096342`)
- **models** : the application fetches its own models (`f33a1ad`)
- **voices** : split two voices joined after the meeting (`2cad762`)
- **minutes** : the instructions given during the meeting reach the writer (`8c32ea5`)
- **update** : install the published binary for this system (`e04b31e`)
- **mail** : check the send will work at the start of the meeting (`c0a43a6`)
- **assistant** : choose its first name from a list that has been tested (`17270a0`)
- **windows** : write the entry point the executable needs (`a607684`)
- **assistant** : give it the documents, and let the exchange run its course (`adc6176`)
- **voice** : pick the voice by ear, and gain a factor of ten on the way (`493264a`)
- **assistant** : let the exchange continue, and separate voice from taking part (`8b04218`)
- **direct** : put the document button where the meeting is (`3139eb8`)
- **voices** : replay the stitching without transcribing an hour again (`6a4fb93`)
- **assistant** : let it raise a point, and mostly decide not to (`f0b6231`)
- **assistant** : download its voice, and settle its name in the window (`af86ccd`)
- **assistant** : give it a voice, a name, and the sense to keep quiet (`94d79c4`)
- **conversation** : teach it and feed it documents while the meeting runs (`aeeb533`)
- **sources** : read and write a registered GitLab or Jira, never beyond it (`73fb08e`)
- **depot** : drop audio, video or documents and get a proposed filing (`860c07f`)
- **contexte** : teach it in a sentence, and confirm before writing (`d6a6c40`)
- **consentement** : say in the report what the participants were told (`98048f3`)
- **carte** : stop the duplicates, mark what was decided, read what others add (`1bfa2b6`)
- **niveau** : judge speech level, not silence, and say it during the meeting (`819bc7d`)
- **recuperer** : rebuild a meeting from the live thread, and disarm --quand-meme (`3adc6bc`)
- **sauvegarde** : copy the work, since a lost disk took everything (`6ebe9b2`)
- **calibrage** : measure the threshold across two sittings, not within one (`ac00b1d`)
- **carte** : build a subject's map from a meeting, and publish it (`0f7fdae`)
- **carte** : publish a subject map to Miro as native objects (`9217966`)
- **carte** : the rules for a subject map that a meeting completes (`0ea3ce7`)
- **maj** : install an update in one click, without losing anything (`a7edaab`)
- **retention** : keep recordings under an explicit rule, not a growing folder (`288a9c5`)
- **conversation** : keep the conversation, and prove an update loses nothing (`699d8ff`)
- **conversation** : let the assistant search, and never the report writer (`52ec371`)
- **version** : know which version is running, and whether a newer one exists (`6b9984a`)
- **fenetre** : make a failed report recoverable in one click (`fe2f8a8`)
- **conversation** : queue the tool's questions, and count them on the tab (`679ea60`)
- **questions** : decide what the tool may legitimately ask about (`50e9473`)
- **conversation** : answer from the live thread while the meeting runs (`145f73a`)
- **reunions** : let the meeting name itself from its report (`635982b`)
- **contexte** : tell the tool what a model cannot guess (`11bca07`)
- **reunions** : name a meeting, and forget one for good (`3b16d87`)
- **installation** : write the language the machine announces, not "fr" (`1bb4319`)
- **langue** : ask for the language at install, and let the report follow it (`f225504`)
- **domaine** : make the language a domain value instead of an assumption (`2ecf27c`)
- **transcription** : make the graphics card usable on Linux (`b88fe79`)
- Greffier, a local-first meeting assistant (`d601adf`)

## Fixed

- **diagnostic** : ask the driver what the machine can compute with (`67d11e9`)
- **cuda** : read the card the way each of the three systems files it (`3530763`)
- **cuda** : keep the card when faster-whisper brings its own onnx runtime (`0dd07d4`)
- **window** : the banner ate every screen, and eight buttons had one weight (`f876794`)
- **voices** : her own answers were counted as a participant (`bdb96b1`)
- **mypy** : one exception protected nothing, another was missing (`6b897eb`)
- **devices** : somebody else's headset was the default microphone (`0819b35`)
- **window** : it refused to open on any Python shipped by a distribution (`f0597f6`)
- **dev** : mypy could not follow uvicorn, absent from the dev extra (`7f79a55`)
- **install** : the window's text was not antialiased on Linux (`43c3773`)
- **window** : ending a meeting froze the window instead of showing the end (`8d72ae1`)
- **settings** : no environment variable ever reached a configured machine (`f0cc40b`)
- **install** : the command the README announces was not in the PATH (`01aecfb`)
- **install** : a dead link where the skills go stopped the installation (`357b1a0`)
- **voices** : a voice holding two people poisons the bank (`f45a160`)
- **live** : a fragment of a voice took a row next to the people (`a425a33`)
- **transcription** : cut the loop the model writes when it loses the thread (`ad38092`)
- **cli** : the hardware watch is launched by a name that no longer exists (`64d98fe`)
- **questions** : stop putting the same question to the room at every launch (`a8cd77a`)
- **assistant** : nothing to answer is silence, not a sentence saying so (`6461a87`)
- **assistant** : it answered to "lui", and waited fifteen seconds to answer at all (`fcd43f5`)
- **live** : a full thread lends nobody's name to a stranger (`8351dc7`)
- **architecture** : the rules were reading a folder that no longer existed (`a5a86b0`)
- **assistant** : it could not search, while its guidance said it could (`54a24e4`)
- **macos** : the build hung for an hour on a password window (`a6690fa`)
- **install** : the installer died on its first line, then declared the model broken (`1a59fcb`)
- **live** : one label per person, and scraps announced with the others (`676e435`)
- **tools** : a cache written by an older version stops nothing (`8c9c4f8`)
- **tools** : the argument names argparse carries in strings (`5539cf9`)
- **assistant** : stay silent once the meeting is over (`c4016dc`)
- **assistant** : make hearing itself unable to start a loop (`5bde9c5`)
- **transcription** : collapse the model's repeat loop (`9a62785`)
- **mail** : let Outlook take its time, and say which failure it was (`07250b9`)
- **build** : refuse a framework interpreter instead of half-building (`dd891cd`)
- **assistant** : the two controls that decide how it takes part (`ee5b007`)
- **minutes** : agree the verb on the participants line (`7f18170`)
- **build** : relaunch the app for real, and check that it came back (`0777adb`)
- **state** : a failure must stop the state from lying (`dd16d3e`)
- **minutes** : two voices under the same name are one person (`3a3a4e7`)
- **direct** : make joining two voices reversible (`0c1af44`)
- **voice** : cutting means silence, not skipping a sentence (`7d8f4ec`)
- **voice** : cut the sound when the button is pressed, not fifteen seconds later (`092ad27`)
- **direct** : correct itself in session, on what the bank already said (`6abaff4`)
- **assistant** : one button for the voice, and stop hiding the on switch (`72bd479`)
- **build** : rebuilding also means relaunching (`8d0499a`)
- **direct** : stop founding a voice per sentence, and measure why it did (`a814898`)
- **direct** : follow both buttons without restarting the meeting (`0c6df74`)
- **questions** : stop asking things whose answer changes nothing (`2d41323`)
- **installer** : recognise a voice model whose file is not named model.onnx (`02121a2`)
- **window** : use a cursor that exists outside macOS (`a3266cb`)
- **assistant** : stop paraphrasing a sentence written to be said (`714fcff`)
- **types** : settle what a night of --no-verify let through (`069ec96`)
- **state** : notice that a processing run died, not only a recording (`d939a96`)
- **channels** : stop announcing that everyone is missing when nobody is (`17e790d`)
- **tests** : give the test dialogue voices a model can actually hear (`6071ad8`)
- **linux** : stop taking a directory for a working environment (`49e85ff`)
- **bank** : repair a bank at the grain that matters, the print (`fed1f54`)
- **update** : make the update button able to work at all (`601d4c1`)
- **minutes** : count the people present, and notice a writing left undone (`ef3255a`)
- **names** : make naming a voice reversible, and stop it poisoning the bank (`60f7e71`)
- **voices** : stitch fragments onto the voices that carry the meeting (`495ec40`)
- **direct** : give the model the minute before the slice, so the word is right (`fc02b4b`)
- **direct** : a term learned mid-meeting serves the next sentence (`882c229`)
- **reglages** : say what the button updates, and reread the account on return (`71d7fe7`)
- **empreintes** : lower the recognition threshold to 0.45, measured (`8149c4b`)
- **micro** : prefer a headset that hears anything, not the loudest one (`94deb09`)
- **fenetre** : make the action bar a proper grid, evenly spread (`2e9140b`)
- **fenetre** : let the action bar wrap, and give the tool a way to see it (`79238e1`)
- **carte** : complete a map instead of doubling it at every pass (`9f2e954`)
- **fenetre** : keep the startup notice out of a meeting's conversation (`5e5fe5e`)
- **journal** : write the session start, so the log says something at all (`cd92f08`)
- **veille** : say it while the meeting is running when nothing is recorded (`105f6b1`)
- **compte-rendu** : report the clock times the recording kept (`7b29b95`)
- **depot** : order meetings by when they were held (`be59155`)
- **traiter** : keep the meeting before writing the report (`7171fbe`)
- **fichiers** : stop collapsing non-Latin names to the same identifier (`ee197f7`)
- **traiter** : keep the meeting when no report is written (`eed7a82`)
- **diagnostic** : judge sound capture on the server, not on pactl (`a9f3091`)
- **interface** : size fonts in pixels and name families Tk guarantees (`065a272`)
- **transcription** : fall back to the processor when the card cannot compute (`c86872b`)

## Performance

- **voiceprints** : stop reopening the hundred-megabyte model at every click (`61b093b`)
- **voice** : stop opening the voice model at every question (`77e2bb9`)
- **transcription** : open the model while the encoder is still closing (`ea50fad`)
- **voice** : let the assistant speak from the graphics card (`5556f0f`)
- **diarisation** : cut the speaker turns on the graphics card (`9e08737`)
- **assistant** : keep the machine's MCP servers out of the call (`b47f7ab`)
- **locuteurs** : give the models half the cores instead of one (`c36b9bf`)

## Documentation

- record that the history no longer names anybody (`5cd1e2d`)
- **cuda** : write down what was measured, and what did not help (`138f3ab`)
- **readme** : a document one can find things in (`9435bf0`)
- **contributing** : the conventions a patch is expected to follow (`ea96b0b`)
- **tests** : the email, the capture, the context, Miro and the locations (`e57d1bc`)
- **tests** : the email, the capture, the context, Miro and the locations (`9365f78`)
- **tests** : the settings, the hardware and the intents, in English (`80c3483`)
- **tests** : the fallback, the attribution, the level and the assistant (`e597d6d`)
- **tests** : the levels, the voiceprint bounds, tidying and the email (`7b0f7a8`)
- **tests** : the store, the backups, naming a voice and rendering (`cabea3a`)
- **tests** : the subject board, the chosen language and the settings (`b60b737`)
- **tests** : the credits, the channels, the data outside the bundle (`74abe72`)
- **tests** : the sending probe, the bank across meetings and the whole chain (`ca61db2`)
- **tests** : the voice, the hard cases, the models and the layers, in English (`f4a3b5f`)
- **tests** : the style, the SMTP session and the updates, in English (`f46e18f`)
- **tests** : the last comment of the recording tests (`240ea29`)
- **tests** : the names and the recording, in English (`72afcdb`)
- **tests** : the questions and the meeting in a room, in English (`9d0b56f`)
- **tests** : the installer and the voice bank, in English (`a7a8508`)
- **tests** : the integration tests' prose, in English (`210d293`)
- **tests** : the manners and the settings, in English (`fcc44de`)
- **tests** : the window and the conversation harness, in English (`9dcb84d`)
- **tests** : the channels and the audio hardware, in English (`a7a5db1`)
- **tests** : the published thread's prose, in English (`f3a07bf`)
- **tests** : the live thread's prose, in English (`4462aa0`)
- **src** : the last French prose outside what the software shows (`3d6eba6`)
- **tests** : the assistant's prose, in English (`e3342f7`)
- **tests** : the voiceprint matching prose, in English (`87a8749`)
- **tests** : the processing chain's prose, in English (`c71a936`)
- name the diagrams in English, and show the one nobody could see (`970e4e0`)
- the window, the command line and the wiring, in English (`fb7e536`)
- **adapters** : the tools, in English (`89c2b59`)
- **adapters** : the nine biggest adapters, in English (`1cbebc1`)
- **application** : the remaining use cases, in English (`032d44a`)
- say the ports and the application in English (`b043b2c`)
- **domain** : say the domain in English, in one line each (`fba2fa4`)
- **rex** : what the meeting of 2026-09-10 actually measured (`e024ee3`)
- ouvrir l'étude de la séparation des voix, et le banc qui la tranchera (`c835690`)
- ouvrir le retour d'expérience de la réunion du 10, avec ses chiffres (`b69812b`)
- record what the first morning of use showed, and what it left open (`203c9e0`)
- **publier** : sortir le workflow du chemin que le jeton ne peut pas écrire (`60a0a5d`)
- **skill** : teach the repair skill what this week added (`3379450`)
- **readme** : say that it takes part, and how to keep it quiet (`6fc8d1b`)
- record the night the tool learned to take part (`3222c1c`)
- **sauvegarde** : a synchronised folder sends the data to its host (`13a4f14`)
- **calibrage** : four series say the threshold is right, one series said wrong (`f16c044`)
- **skills** : write down how to assist a meeting while it runs (`1120e73`)
- name the machine's guard by its role, not by its product (`49e403b`)
- write the engineering log and the calibration notes in English (`6682388`)
- **readme** : describe the tree that exists, and say what enforces it (`5011a68`)
- **assets** : redraw the two diagrams in English, and fix what they hid (`a927ce4`)
- **readme** : write the README in English (`63fe6f9`)
- measure the chain against a real four-person meeting (`0b7d8bf`)
- **readme** : stop claiming the chain runs on a Mac's graphics card (`5b45a42`)
- record what real recordings showed, and what the run costs (`9be2fcc`)
- record the first run on a real Linux desktop (`8b220d2`)

## Refactored

- **cuda** : gather the loading of the nvidia libraries in one adapter (`d44c7c8`)
- **layers** : two primary adapters were leaning on each other (`15d1ca0`)
- **install** : the code, in English; the messages, in French (`71d1a6d`)
- **domain** : the edit distance comes from a library (`e8e02d4`)
- the French words the rename had left in every layer (`c75ff1d`)
- **domain** : the thresholds, the live thread and the questions (`f8d72b9`)
- **domain** : English identifiers in the domain layer (`91b8aef`)
- the tools directory and its files, in English (`259a3c3`)
- name the remaining classes in English (`42bd8d5`)
- drop the comment blocks, and say the domain in English (`43c9190`)
- **tests** : name the test files after the modules they exercise (`9dfb70a`)
- the code speaks English (`b598fe4`)
- **config** : pin the file keys with validation aliases (`64ba326`)
- drop the field comment blocks (`5807d54`)
- **traiter** : name the report title import for what it is (`12dc089`)
- **couches** : put every module in the layer its imports say it belongs to (`abea9d4`)

