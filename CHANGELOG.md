# Changelog

Generated from the commit messages, which follow the Angular convention:

    python3 tools/changelog.py > CHANGELOG.md

## New

- **install**: fetch the sherpa-onnx wheel that talks to the card (`765d9cb`)
- **graph**: an index of what the tool knows, and a preparation that opens on it (`4cc96fa`)
- **dictation**: one click to speak, and the silence ends the sentence (`0c4c3c4`)
- **window**: preparation has its own tab, and the threads can be cleared (`5cef5e0`)
- **tongue**: the tool speaks the language of the machine it runs on (`e345a6f`)
- **window**: the Conversation tab prepares the next meeting, out loud (`b07ef1e`)
- **prepare**: answering somebody who is preparing a meeting (`86e5ea4`)
- **dictation**: a microphone held down, and a sound saying she heard (`938dc19`)
- **preparation**: a meeting prepared before it is held, and taken by it (`b4c8295`)
- **api**: an HTTP door, so a site can drive the tool (`d36d751`)
- **memory**: what earlier meetings left now reaches the next one (`063d803`)
- **assistant**: a discreet sound when she looks something up (`2cdb899`)
- **window-proof**: photograph the window on X11 too, not only on macOS (`bf905e1`)
- **tests**: the whole chain proves itself on Linux too (`bdc09ef`)
- **minutes**: the names given during the meeting reach the minutes (`8eb9464`)
- **assistant**: give it the glossary of the setting, not only the writer (`ea07c22`)
- **diagnostic**: look at the models and the voice bank (`91764e7`)
- **models**: the application fetches its own models (`ec58a54`)
- **voices**: split two voices joined after the meeting (`7f7beb3`)
- **minutes**: the instructions given during the meeting reach the writer (`184aab6`)
- **update**: install the published binary for this system (`862bf99`)
- **mail**: check the send will work at the start of the meeting (`b580928`)
- **assistant**: choose its first name from a list that has been tested (`7e65dbc`)
- **windows**: write the entry point the executable needs (`c04d03d`)
- **assistant**: give it the documents, and let the exchange run its course (`85a0efa`)
- **voice**: pick the voice by ear, and gain a factor of ten on the way (`ebe5982`)
- **assistant**: let the exchange continue, and separate voice from taking part (`7303b6f`)
- **direct**: put the document button where the meeting is (`cf72804`)
- **voices**: replay the stitching without transcribing an hour again (`91816e4`)
- **assistant**: let it raise a point, and mostly decide not to (`82d07e7`)
- **assistant**: download its voice, and settle its name in the window (`5563812`)
- **assistant**: give it a voice, a name, and the sense to keep quiet (`b85bed6`)
- **conversation**: teach it and feed it documents while the meeting runs (`2cf63d8`)
- **sources**: read and write a registered GitLab or Jira, never beyond it (`e82cfa4`)
- **depot**: drop audio, video or documents and get a proposed filing (`3db44a1`)
- **contexte**: teach it in a sentence, and confirm before writing (`154427f`)
- **consentement**: say in the report what the participants were told (`66a9c11`)
- **carte**: stop the duplicates, mark what was decided, read what others add (`2540794`)
- **niveau**: judge speech level, not silence, and say it during the meeting (`1a68a3d`)
- **recuperer**: rebuild a meeting from the live thread, and disarm --quand-meme (`4753b81`)
- **sauvegarde**: copy the work, since a lost disk took everything (`24346cc`)
- **calibrage**: measure the threshold across two sittings, not within one (`2396803`)
- **carte**: build a subject's map from a meeting, and publish it (`2197374`)
- **carte**: publish a subject map to Miro as native objects (`40e4f71`)
- **carte**: the rules for a subject map that a meeting completes (`16c8725`)
- **maj**: install an update in one click, without losing anything (`1e76528`)
- **retention**: keep recordings under an explicit rule, not a growing folder (`19bf2a9`)
- **conversation**: keep the conversation, and prove an update loses nothing (`0a36e71`)
- **conversation**: let the assistant search, and never the report writer (`17ad1d2`)
- **version**: know which version is running, and whether a newer one exists (`cd25597`)
- **fenetre**: make a failed report recoverable in one click (`1b26b92`)
- **conversation**: queue the tool's questions, and count them on the tab (`17d47c2`)
- **questions**: decide what the tool may legitimately ask about (`08c894b`)
- **conversation**: answer from the live thread while the meeting runs (`a58d1cb`)
- **reunions**: let the meeting name itself from its report (`e10e134`)
- **contexte**: tell the tool what a model cannot guess (`99bfeec`)
- **reunions**: name a meeting, and forget one for good (`4cb30e3`)
- **installation**: write the language the machine announces, not "fr" (`f8e2bc2`)
- **langue**: ask for the language at install, and let the report follow it (`4787d2d`)
- **domaine**: make the language a domain value instead of an assumption (`4296ba7`)
- **transcription**: make the graphics card usable on Linux (`7e18927`)
- Greffier, a local-first meeting assistant (`d601adf`)

## Fixed

- **diagnostic**: ask the driver what the machine can compute with (`33bdab0`)
- **cuda**: read the card the way each of the three systems files it (`9993183`)
- **cuda**: keep the card when faster-whisper brings its own onnx runtime (`44b9a31`)
- **window**: the banner ate every screen, and eight buttons had one weight (`ff9756e`)
- **voices**: her own answers were counted as a participant (`eeb4cd0`)
- **mypy**: one exception protected nothing, another was missing (`02fadf3`)
- **devices**: somebody else's headset was the default microphone (`8b6d66e`)
- **window**: it refused to open on any Python shipped by a distribution (`0085cca`)
- **dev**: mypy could not follow uvicorn, absent from the dev extra (`7ad9f8f`)
- **install**: the window's text was not antialiased on Linux (`a900776`)
- **window**: ending a meeting froze the window instead of showing the end (`f4fbc31`)
- **settings**: no environment variable ever reached a configured machine (`62036e7`)
- **install**: the command the README announces was not in the PATH (`f9b9085`)
- **install**: a dead link where the skills go stopped the installation (`dfdc718`)
- **voices**: a voice holding two people poisons the bank (`b9db81a`)
- **live**: a fragment of a voice took a row next to the people (`bdcb8f4`)
- **transcription**: cut the loop the model writes when it loses the thread (`eeffe54`)
- **cli**: the hardware watch is launched by a name that no longer exists (`3bedbd5`)
- **questions**: stop putting the same question to the room at every launch (`bcaab27`)
- **assistant**: nothing to answer is silence, not a sentence saying so (`0a1b49b`)
- **assistant**: it answered to "lui", and waited fifteen seconds to answer at all (`9241a8d`)
- **live**: a full thread lends nobody's name to a stranger (`1c25c63`)
- **architecture**: the rules were reading a folder that no longer existed (`6b6c644`)
- **assistant**: it could not search, while its guidance said it could (`6332770`)
- **macos**: the build hung for an hour on a password window (`abbd01b`)
- **install**: the installer died on its first line, then declared the model broken (`579a3a2`)
- **live**: one label per person, and scraps announced with the others (`60e359d`)
- **tools**: a cache written by an older version stops nothing (`3e649de`)
- **tools**: the argument names argparse carries in strings (`f059e0a`)
- **assistant**: stay silent once the meeting is over (`c9fac5d`)
- **assistant**: make hearing itself unable to start a loop (`f8bd732`)
- **transcription**: collapse the model's repeat loop (`127d2fe`)
- **mail**: let Outlook take its time, and say which failure it was (`91def1b`)
- **build**: refuse a framework interpreter instead of half-building (`a9da33a`)
- **assistant**: the two controls that decide how it takes part (`4fdb1f6`)
- **minutes**: agree the verb on the participants line (`368cfe0`)
- **build**: relaunch the app for real, and check that it came back (`f246063`)
- **state**: a failure must stop the state from lying (`72be477`)
- **minutes**: two voices under the same name are one person (`9eb3fac`)
- **direct**: make joining two voices reversible (`2bd3877`)
- **voice**: cutting means silence, not skipping a sentence (`b7bfad3`)
- **voice**: cut the sound when the button is pressed, not fifteen seconds later (`7d8caca`)
- **direct**: correct itself in session, on what the bank already said (`8278757`)
- **assistant**: one button for the voice, and stop hiding the on switch (`ee53b6d`)
- **build**: rebuilding also means relaunching (`7b8d47b`)
- **direct**: stop founding a voice per sentence, and measure why it did (`4c13c76`)
- **direct**: follow both buttons without restarting the meeting (`6a263a4`)
- **questions**: stop asking things whose answer changes nothing (`a446305`)
- **installer**: recognise a voice model whose file is not named model.onnx (`6dbab30`)
- **window**: use a cursor that exists outside macOS (`21a4965`)
- **assistant**: stop paraphrasing a sentence written to be said (`03b52cc`)
- **types**: settle what a night of --no-verify let through (`71cc1ee`)
- **state**: notice that a processing run died, not only a recording (`7b0c665`)
- **channels**: stop announcing that everyone is missing when nobody is (`2654bee`)
- **tests**: give the test dialogue voices a model can actually hear (`21e8a43`)
- **linux**: stop taking a directory for a working environment (`47f0ed1`)
- **bank**: repair a bank at the grain that matters, the print (`fd49c32`)
- **update**: make the update button able to work at all (`ec37eb7`)
- **minutes**: count the people present, and notice a writing left undone (`5b515c4`)
- **names**: make naming a voice reversible, and stop it poisoning the bank (`88f33c5`)
- **voices**: stitch fragments onto the voices that carry the meeting (`9e9ef3d`)
- **direct**: give the model the minute before the slice, so the word is right (`4df6f81`)
- **direct**: a term learned mid-meeting serves the next sentence (`18b446f`)
- **reglages**: say what the button updates, and reread the account on return (`093d983`)
- **empreintes**: lower the recognition threshold to 0.45, measured (`13042dd`)
- **micro**: prefer a headset that hears anything, not the loudest one (`aee3c5c`)
- **fenetre**: make the action bar a proper grid, evenly spread (`fac8996`)
- **fenetre**: let the action bar wrap, and give the tool a way to see it (`ec084f2`)
- **carte**: complete a map instead of doubling it at every pass (`3e4c844`)
- **fenetre**: keep the startup notice out of a meeting's conversation (`29d2501`)
- **journal**: write the session start, so the log says something at all (`6e283fb`)
- **veille**: say it while the meeting is running when nothing is recorded (`9d0f2aa`)
- **compte-rendu**: report the clock times the recording kept (`34ef0e2`)
- **depot**: order meetings by when they were held (`2c5aced`)
- **traiter**: keep the meeting before writing the report (`e8d3b82`)
- **fichiers**: stop collapsing non-Latin names to the same identifier (`0ab9310`)
- **traiter**: keep the meeting when no report is written (`5f357ab`)
- **diagnostic**: judge sound capture on the server, not on pactl (`2679739`)
- **interface**: size fonts in pixels and name families Tk guarantees (`065a272`)
- **transcription**: fall back to the processor when the card cannot compute (`c86872b`)

## Performance

- **voiceprints**: stop reopening the hundred-megabyte model at every click (`7dd882a`)
- **voice**: stop opening the voice model at every question (`5e05600`)
- **transcription**: open the model while the encoder is still closing (`0c680e9`)
- **voice**: let the assistant speak from the graphics card (`02ea861`)
- **diarisation**: cut the speaker turns on the graphics card (`8f418ff`)
- **assistant**: keep the machine's MCP servers out of the call (`bc0b336`)
- **locuteurs**: give the models half the cores instead of one (`13cb53b`)

## Documentation

- record that the history no longer names anybody (`f80dfde`)
- **cuda**: write down what was measured, and what did not help (`410106d`)
- **readme**: a document one can find things in (`beac2e5`)
- **contributing**: the conventions a patch is expected to follow (`4bf8ae4`)
- **tests**: the email, the capture, the context, Miro and the locations (`5ac5557`)
- **tests**: the email, the capture, the context, Miro and the locations (`9032eb7`)
- **tests**: the settings, the hardware and the intents, in English (`6f3f558`)
- **tests**: the fallback, the attribution, the level and the assistant (`92449ad`)
- **tests**: the levels, the voiceprint bounds, tidying and the email (`a71754d`)
- **tests**: the store, the backups, naming a voice and rendering (`e2de8d5`)
- **tests**: the subject board, the chosen language and the settings (`3020891`)
- **tests**: the credits, the channels, the data outside the bundle (`bc32a4c`)
- **tests**: the sending probe, the bank across meetings and the whole chain (`fc5d4b8`)
- **tests**: the voice, the hard cases, the models and the layers, in English (`57098f4`)
- **tests**: the style, the SMTP session and the updates, in English (`8d2a585`)
- **tests**: the last comment of the recording tests (`9a47244`)
- **tests**: the names and the recording, in English (`ff8a4cf`)
- **tests**: the questions and the meeting in a room, in English (`4eaf982`)
- **tests**: the installer and the voice bank, in English (`a993a52`)
- **tests**: the integration tests' prose, in English (`05c6875`)
- **tests**: the manners and the settings, in English (`ad7d0f2`)
- **tests**: the window and the conversation harness, in English (`9886b6c`)
- **tests**: the channels and the audio hardware, in English (`8e15874`)
- **tests**: the published thread's prose, in English (`2bcf9d5`)
- **tests**: the live thread's prose, in English (`82d3264`)
- **src**: the last French prose outside what the software shows (`916231a`)
- **tests**: the assistant's prose, in English (`dfb9182`)
- **tests**: the voiceprint matching prose, in English (`e977f8e`)
- **tests**: the processing chain's prose, in English (`9813c1a`)
- name the diagrams in English, and show the one nobody could see (`a2c4011`)
- the window, the command line and the wiring, in English (`9abc073`)
- **adapters**: the tools, in English (`bfbdc21`)
- **adapters**: the nine biggest adapters, in English (`6d2a7a3`)
- **application**: the remaining use cases, in English (`1318a3c`)
- say the ports and the application in English (`ed235fb`)
- **domain**: say the domain in English, in one line each (`6cf016f`)
- **rex**: what the meeting of 2026-09-10 actually measured (`f9b877f`)
- ouvrir l'étude de la séparation des voix, et le banc qui la tranchera (`7f88e25`)
- ouvrir le retour d'expérience de la réunion du 10, avec ses chiffres (`272a180`)
- record what the first morning of use showed, and what it left open (`8b6f625`)
- **publier**: sortir le workflow du chemin que le jeton ne peut pas écrire (`32f7b1e`)
- **skill**: teach the repair skill what this week added (`9b46091`)
- **readme**: say that it takes part, and how to keep it quiet (`06b5afe`)
- record the night the tool learned to take part (`ed3a196`)
- **sauvegarde**: a synchronised folder sends the data to its host (`f2f2a1e`)
- **calibrage**: four series say the threshold is right, one series said wrong (`6226d54`)
- **skills**: write down how to assist a meeting while it runs (`9254c81`)
- name the machine's guard by its role, not by its product (`096333b`)
- write the engineering log and the calibration notes in English (`ad91ea5`)
- **readme**: describe the tree that exists, and say what enforces it (`2df9204`)
- **assets**: redraw the two diagrams in English, and fix what they hid (`86dff3b`)
- **readme**: write the README in English (`eebb619`)
- measure the chain against a real four-person meeting (`7df54c9`)
- **readme**: stop claiming the chain runs on a Mac's graphics card (`63761ef`)
- record what real recordings showed, and what the run costs (`d18a05c`)
- record the first run on a real Linux desktop (`cfba596`)

## Refactored

- **cuda**: gather the loading of the nvidia libraries in one adapter (`37f4a67`)
- **layers**: two primary adapters were leaning on each other (`bf8a286`)
- **install**: the code, in English; the messages, in French (`28549c4`)
- **domain**: the edit distance comes from a library (`c47fb91`)
- the French words the rename had left in every layer (`3768b1e`)
- **domain**: the thresholds, the live thread and the questions (`2ac1443`)
- **domain**: English identifiers in the domain layer (`9f30d48`)
- the tools directory and its files, in English (`03793ef`)
- name the remaining classes in English (`5fe1908`)
- drop the comment blocks, and say the domain in English (`7dbd9f9`)
- **tests**: name the test files after the modules they exercise (`a7b94ef`)
- the code speaks English (`16def1b`)
- **config**: pin the file keys with validation aliases (`34428d2`)
- drop the field comment blocks (`0fa421e`)
- **traiter**: name the report title import for what it is (`90dfa40`)
- **couches**: put every module in the layer its imports say it belongs to (`5450567`)

