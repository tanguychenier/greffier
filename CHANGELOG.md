# Journal des modifications

Engendré depuis les messages de commit (convention Angular) :

    python3 outils/journal_des_modifications.py > CHANGELOG.md

## Nouveautés

- **presence** — prove the chain on a table meeting, and stop giving a straddling sentence to the loudest (`b868c2b`)
- **settings** — pick the language from a list, and diagram the app in the README (`31c62f0`)
- **window** — a real accent, drawn dropdowns, and a contextual account action (`39f94d3`)
- **settings** — a Réglages tab that writes config.toml, and an explicit writer model (`26763de`)
- **macos** — self-contained app bundle, signed with a stable identity (`23954d3`)
- **restitution** — regenerate the report after naming a voice, not the whole chain (`4cf4d95`)
- **window** — serif title and eased level meters (`99b9328`)
- **window** — make the recording dot breathe (`56132f3`)
- **live** — show what is said during the meeting, and let it be corrected (`f3710cf`)
- **mail** — send the report, instead of asking a second time (`b65158a`)
- **audio** — set the machine up on its own, instead of asking (`bb977d5`)
- **interface** — a window that runs, replacing three half-interfaces (`5fa96ad`)
- **record** — adapt to hardware changes while the meeting is running (`d585303`)
- **devices** — read the real audio hardware, cheaply enough to watch it (`dbde54a`)
- **devices** — decide what to do when the audio hardware changes mid-meeting (`28dbb81`)
- **mail** — give the report a summary and a real header (`deef6e8`)
- **cli** — add a send command that shows the mail before sending it (`d1bac78`)
- **macos** — name voices and act on findings from the panel (`cd469c5`)
- **tickets** — turn decided actions into tickets ready to open (`a4ca7e5`)
- **assist** — watch the meeting and propose, never act (`010ac59`)
- **tray** — add the notification-area icon for Linux and Windows (`036b2ee`)
- **report** — warn about what the transcript lost, and restitute beyond text (`0c9e83a`)
- **setup** — add a first-run configuration wizard (`4c39c94`)
- **macos** — bring audio device creation into the project (`471c40e`)
- **macos** — bring the menu bar icon into the repository (`01ec1de`)
- **record** — add the recording state machine and the remaining adapters (`205ed9a`)
- **voices** — remember voices across meetings (`191c991`)
- **cli** — run the whole chain from audio to report (`56d2914`)
- **install** — support Linux and Windows, install what is missing (`78c0bf2`)
- **voiceprints** — merge over-split voices and raise the threshold (`0b5add3`)
- **voiceprints** — match voices across meetings (`8a29839`)
- **names** — infer speaker names from spoken mentions (`b6dbb9a`)
- **domain** — add the meeting model (`605d34e`)

## Corrections

- **window** — keep long replies in their column, and name what the fields expect (`f87a03b`)
- **paths** — keep the Tcl lookup where continuous integration can test it (`2e4b66e`)
- **live** — run the model the machine can afford, and stop bribes founding voices (`ca3a62b`)
- **live** — never invent more voices than the announced participants (`0db351e`)
- **transcription** — drop the credits the model invents on a weak signal (`5cf9271`)
- **chain** — keep the meeting on disk, and dictate the context line (`2464c24`)
- **live** — re-merge voices as material accumulates, instead of never (`445fd43`)
- **voices** — stop asserting a name when the bank contradicts itself (`872beba`)
- **settings** — apply every change at once, and show the writer's account (`73be8a3`)
- **macos** — put the stable user bin path first, so the writer keeps its rule (`592f4c4`)
- **installer** — reuse the app's locations and move leftovers out of hidden folders (`988113b`)
- **config** — one definition of the config and data locations, native on macOS (`5d0a242`)
- **config** — store data in Application Support on macOS, not ~/.local (`c57b9c7`)
- **macos** — ship the device lister inside the bundle, not in ~/.local (`5147a91`)
- **macos** — only hijack the bare double-click launch, not every invocation (`757235a`)
- **mail** — speak implicit TLS on port 465, STARTTLS elsewhere (`c02037f`)
- **interface** — closing the window must never silently kill a meeting (`f3cf4a4`)
- **macos** — stop trusting LSEnvironment for PATH, it silently drops it (`441457a`)
- **macos** — carry the real PATH into the packaged app (`0b3b24e`)
- **interface** — stop resizing on tab switch, draw a scrollbar that matches (`874a9ea`)
- **macos** — name the process Greffier, not python3 (`277efbc`)
- **direct** — trim duplicated text at slice boundaries (`2f9a95d`)
- **nommer** — keep a detected proposition even on a short voice (`ab9520f`)
- **diarisation** — correct the raw clustering threshold, measured (`14c217f`)
- **window** — launch live transcription when recording starts from the window (`8b976d7`)
- **voiceprints** — cap the extract, or a long turn brings the model down (`76b23e7`)
- **diarisation** — a laptop on a table is not a video call (`d845d04`)
- **audio** — listen to each microphone before trusting it (`0f73f80`)
- **macos** — install the app where the system can find it (`53b519d`)
- **transcription** — bring the audio up to level, or the model invents words (`4652db1`)
- **chain** — surface low coverage, publish progress, reject common words (`6279397`)
- **diarisation** — stop averaging the channels, and get the local voice back (`08e6181`)
- **report** — stop demanding the dash the brief forbids, and the missing date (`efedebf`)
- **names** — a name backed only by calls-to-speak is proposed, never asserted (`425995b`)
- **mail** — drop the footer disclaimer (`08c7eb8`)
- **ci** — pin jobs to the docker runners (`dcaf28e`)
- **mail** — stop attaching the report and the full transcript (`4f638db`)
- **report** — stop claiming a report was sent when nothing was sent (`8bb06fb`)
- **mail** — render the report as UTF-8 HTML, not MacRoman markdown (`d8011ac`)
- **types** — make the whole package pass mypy, and fix a port that lied (`28e512e`)
- **macos** — react to a click on the Dock icon, and log what the interface does (`e58a59b`)
- **report** — restore the CLI assistant as the default writer, configure through .env (`a0c73eb`)
- **install** — make the installer safe on Windows and integrate with the desktop (`b32810f`)

## Documentation

- **changelog** — regenerate from the log (`a00dbb4`)
- **changelog** — regenerate from the log (`2c1ce0a`)
- **changelog** — regenerate from the log (`d838ede`)
- **changelog** — regenerate from the log (`9b5a1a8`)
- **changelog** — regenerate from the log (`ca7192d`)
- describe the self-contained signed bundle and the native locations (`045ff7a`)
- **readme** — correct the state table, stuck since the project's scaffold (`aef515e`)
- **roadmap** — close the three remaining defects from the initial diagnosis (`0468bf1`)
- **live** — record what the real-time trial measured (`9e452e5`)
- **changelog** — regenerate from the log (`03e1139`)
- **roadmap** — record what real use broke, and correct a false claim (`4ee0aa1`)
- **roadmap** — close the eight batches (`7c34eb0`)
- **readme** — document installation (`d7b65df`)
- **readme** — update port status (`399bbf8`)

## Remaniements

- **channels** — move channel separation where both paths can share it (`c1d2556`)
- **interface** — split what needs Tk from what does not (`b843af8`)
- **report** — demand density, and ban the em dash (`e8f0661`)
- **report** — brief the writer for a meeting report, not an audit of itself (`e06c4c4`)

