# Changelog

Generated from the commit messages, which follow the Angular convention:

    python3 tools/changelog.py > CHANGELOG.md

## New

- **install** — fetch the sherpa-onnx wheel that talks to the card (`67e9771`)
- **graph** — an index of what the tool knows, and a preparation that opens on it (`cab175e`)
- **dictation** — one click to speak, and the silence ends the sentence (`488cb15`)
- **window** — preparation has its own tab, and the threads can be cleared (`b3e2159`)
- **tongue** — the tool speaks the language of the machine it runs on (`f6491d7`)
- **window** — the Conversation tab prepares the next meeting, out loud (`abfb50d`)
- **prepare** — answering somebody who is preparing a meeting (`5113df0`)
- **dictation** — a microphone held down, and a sound saying she heard (`cc2b3a3`)
- **preparation** — a meeting prepared before it is held, and taken by it (`b6af835`)
- **api** — an HTTP door, so a site can drive the tool (`c115abb`)
- **memory** — what earlier meetings left now reaches the next one (`f98d177`)
- **assistant** — a discreet sound when she looks something up (`9a72075`)
- **window-proof** — photograph the window on X11 too, not only on macOS (`d0c04ef`)
- **tests** — the whole chain proves itself on Linux too (`b040e22`)
- **minutes** — the names given during the meeting reach the minutes (`0db4691`)
- **assistant** — give it the glossary of the setting, not only the writer (`e804e19`)
- **diagnostic** — look at the models and the voice bank (`e3fa943`)
- **models** — the application fetches its own models (`9f7ffb2`)
- **voices** — split two voices joined after the meeting (`d761b39`)
- **minutes** — the instructions given during the meeting reach the writer (`bdb3c26`)
- **update** — install the published binary for this system (`8beb849`)
- **mail** — check the send will work at the start of the meeting (`b064de0`)
- **assistant** — choose its first name from a list that has been tested (`9e38608`)
- **windows** — write the entry point the executable needs (`53c5332`)
- **assistant** — give it the documents, and let the exchange run its course (`91627cb`)
- **voice** — pick the voice by ear, and gain a factor of ten on the way (`5c5940a`)
- **assistant** — let the exchange continue, and separate voice from taking part (`0f7ef98`)
- **direct** — put the document button where the meeting is (`ad21a51`)
- **voices** — replay the stitching without transcribing an hour again (`c055c28`)
- **assistant** — let it raise a point, and mostly decide not to (`b3a9a1a`)
- **assistant** — download its voice, and settle its name in the window (`d1bbf4e`)
- **assistant** — give it a voice, a name, and the sense to keep quiet (`395ca97`)
- **conversation** — teach it and feed it documents while the meeting runs (`27cd24a`)
- **sources** — read and write a registered GitLab or Jira, never beyond it (`d586d57`)
- **depot** — drop audio, video or documents and get a proposed filing (`4f25152`)
- **contexte** — teach it in a sentence, and confirm before writing (`87b2eb1`)
- **consentement** — say in the report what the participants were told (`a27c8ef`)
- **carte** — stop the duplicates, mark what was decided, read what others add (`0bf09f6`)
- **niveau** — judge speech level, not silence, and say it during the meeting (`1eb1793`)
- **recuperer** — rebuild a meeting from the live thread, and disarm --quand-meme (`27e6e7f`)
- **sauvegarde** — copy the work, since a lost disk took everything (`635d4a1`)
- **calibrage** — measure the threshold across two sittings, not within one (`13bde7d`)
- **carte** — build a subject's map from a meeting, and publish it (`8825f75`)
- **carte** — publish a subject map to Miro as native objects (`397d7c3`)
- **carte** — the rules for a subject map that a meeting completes (`f85eb5a`)
- **maj** — install an update in one click, without losing anything (`dbffbbe`)
- **retention** — keep recordings under an explicit rule, not a growing folder (`d5fa84e`)
- **conversation** — keep the conversation, and prove an update loses nothing (`466f210`)
- **conversation** — let the assistant search, and never the report writer (`a1756fd`)
- **version** — know which version is running, and whether a newer one exists (`425ed3e`)
- **fenetre** — make a failed report recoverable in one click (`7b95305`)
- **conversation** — queue the tool's questions, and count them on the tab (`f01a330`)
- **questions** — decide what the tool may legitimately ask about (`20de4ec`)
- **conversation** — answer from the live thread while the meeting runs (`c96ae46`)
- **reunions** — let the meeting name itself from its report (`110ed56`)
- **contexte** — tell the tool what a model cannot guess (`f81fc08`)
- **reunions** — name a meeting, and forget one for good (`7517007`)
- **installation** — write the language the machine announces, not "fr" (`3a133a2`)
- **langue** — ask for the language at install, and let the report follow it (`68d268c`)
- **domaine** — make the language a domain value instead of an assumption (`e30bc4e`)
- **transcription** — make the graphics card usable on Linux (`e3e5c50`)
- Greffier, a local-first meeting assistant (`6e31b00`)

## Fixed

- **diagnostic** — ask the driver what the machine can compute with (`d531e1e`)
- **cuda** — read the card the way each of the three systems files it (`e965bfa`)
- **cuda** — keep the card when faster-whisper brings its own onnx runtime (`23a8472`)
- **window** — the banner ate every screen, and eight buttons had one weight (`9534b4e`)
- **voices** — her own answers were counted as a participant (`e286110`)
- **mypy** — one exception protected nothing, another was missing (`44386f5`)
- **devices** — somebody else's headset was the default microphone (`52fce92`)
- **window** — it refused to open on any Python shipped by a distribution (`20495b1`)
- **dev** — mypy could not follow uvicorn, absent from the dev extra (`7f197c1`)
- **install** — the window's text was not antialiased on Linux (`e950c7a`)
- **window** — ending a meeting froze the window instead of showing the end (`cb9728f`)
- **settings** — no environment variable ever reached a configured machine (`88cdba2`)
- **install** — the command the README announces was not in the PATH (`524bd4f`)
- **install** — a dead link where the skills go stopped the installation (`0bad7d9`)
- **voices** — a voice holding two people poisons the bank (`1149d8a`)
- **live** — a fragment of a voice took a row next to the people (`222f65e`)
- **transcription** — cut the loop the model writes when it loses the thread (`aa1b523`)
- **cli** — the hardware watch is launched by a name that no longer exists (`7ad1733`)
- **questions** — stop putting the same question to the room at every launch (`b3fb6ae`)
- **assistant** — nothing to answer is silence, not a sentence saying so (`4dbf94c`)
- **assistant** — it answered to "lui", and waited fifteen seconds to answer at all (`c8dfd91`)
- **live** — a full thread lends nobody's name to a stranger (`cc7f3a7`)
- **architecture** — the rules were reading a folder that no longer existed (`834d80e`)
- **assistant** — it could not search, while its guidance said it could (`8d4471e`)
- **macos** — the build hung for an hour on a password window (`3b5cff6`)
- **install** — the installer died on its first line, then declared the model broken (`07e8d42`)
- **live** — one label per person, and scraps announced with the others (`ec60d2f`)
- **tools** — a cache written by an older version stops nothing (`072c916`)
- **tools** — the argument names argparse carries in strings (`c5d0612`)
- **assistant** — stay silent once the meeting is over (`5ffb1ec`)
- **assistant** — make hearing itself unable to start a loop (`48cf837`)
- **transcription** — collapse the model's repeat loop (`e10ada6`)
- **mail** — let Outlook take its time, and say which failure it was (`7efe36b`)
- **build** — refuse a framework interpreter instead of half-building (`49b4d4c`)
- **assistant** — the two controls that decide how it takes part (`17ce6d2`)
- **minutes** — agree the verb on the participants line (`f6b66e5`)
- **build** — relaunch the app for real, and check that it came back (`12fa14c`)
- **state** — a failure must stop the state from lying (`1f9d492`)
- **minutes** — two voices under the same name are one person (`d7ad88e`)
- **direct** — make joining two voices reversible (`e538723`)
- **voice** — cutting means silence, not skipping a sentence (`ed06d3e`)
- **voice** — cut the sound when the button is pressed, not fifteen seconds later (`b65d131`)
- **direct** — correct itself in session, on what the bank already said (`a128708`)
- **assistant** — one button for the voice, and stop hiding the on switch (`b772831`)
- **build** — rebuilding also means relaunching (`e40d457`)
- **direct** — stop founding a voice per sentence, and measure why it did (`2490bff`)
- **direct** — follow both buttons without restarting the meeting (`c7bddc6`)
- **questions** — stop asking things whose answer changes nothing (`6240763`)
- **installer** — recognise a voice model whose file is not named model.onnx (`1cf0e87`)
- **window** — use a cursor that exists outside macOS (`6b1685c`)
- **assistant** — stop paraphrasing a sentence written to be said (`77f45e1`)
- **types** — settle what a night of --no-verify let through (`7ff60f9`)
- **state** — notice that a processing run died, not only a recording (`fea1932`)
- **channels** — stop announcing that everyone is missing when nobody is (`8956383`)
- **tests** — give the test dialogue voices a model can actually hear (`949ddb7`)
- **linux** — stop taking a directory for a working environment (`a69b533`)
- **bank** — repair a bank at the grain that matters, the print (`311c003`)
- **update** — make the update button able to work at all (`0976f5c`)
- **minutes** — count the people present, and notice a writing left undone (`6ea92da`)
- **names** — make naming a voice reversible, and stop it poisoning the bank (`25b0f5c`)
- **voices** — stitch fragments onto the voices that carry the meeting (`dd13885`)
- **direct** — give the model the minute before the slice, so the word is right (`7bc12c8`)
- **direct** — a term learned mid-meeting serves the next sentence (`4c0c09b`)
- **reglages** — say what the button updates, and reread the account on return (`9de4fd5`)
- **empreintes** — lower the recognition threshold to 0.45, measured (`80240fe`)
- **micro** — prefer a headset that hears anything, not the loudest one (`e3edede`)
- **fenetre** — make the action bar a proper grid, evenly spread (`899da3d`)
- **fenetre** — let the action bar wrap, and give the tool a way to see it (`7315229`)
- **carte** — complete a map instead of doubling it at every pass (`dbf1c60`)
- **fenetre** — keep the startup notice out of a meeting's conversation (`3fc9bc8`)
- **journal** — write the session start, so the log says something at all (`53a3cd6`)
- **veille** — say it while the meeting is running when nothing is recorded (`148152e`)
- **compte-rendu** — report the clock times the recording kept (`8d0bcc4`)
- **depot** — order meetings by when they were held (`7bd1817`)
- **traiter** — keep the meeting before writing the report (`a92a7d6`)
- **fichiers** — stop collapsing non-Latin names to the same identifier (`51c5e09`)
- **traiter** — keep the meeting when no report is written (`b96c977`)
- **diagnostic** — judge sound capture on the server, not on pactl (`d7e7c48`)
- **interface** — size fonts in pixels and name families Tk guarantees (`6294e79`)
- **transcription** — fall back to the processor when the card cannot compute (`c8251a8`)

## Performance

- **voiceprints** — stop reopening the hundred-megabyte model at every click (`ee4db4f`)
- **voice** — stop opening the voice model at every question (`264c4f8`)
- **transcription** — open the model while the encoder is still closing (`9f81899`)
- **voice** — let the assistant speak from the graphics card (`f7a9323`)
- **diarisation** — cut the speaker turns on the graphics card (`6ac0760`)
- **assistant** — keep the machine's MCP servers out of the call (`91deb38`)
- **locuteurs** — give the models half the cores instead of one (`ef5f4d6`)

## Documentation

- **cuda** — write down what was measured, and what did not help (`c3da116`)
- **readme** — a document one can find things in (`f230405`)
- **contributing** — the conventions a patch is expected to follow (`6811b58`)
- **tests** — the email, the capture, the context, Miro and the locations (`5d873a8`)
- **tests** — the email, the capture, the context, Miro and the locations (`5042389`)
- **tests** — the settings, the hardware and the intents, in English (`dd99997`)
- **tests** — the fallback, the attribution, the level and the assistant (`1191bed`)
- **tests** — the levels, the voiceprint bounds, tidying and the email (`ddd7679`)
- **tests** — the store, the backups, naming a voice and rendering (`1e5da56`)
- **tests** — the subject board, the chosen language and the settings (`e4f66cf`)
- **tests** — the credits, the channels, the data outside the bundle (`d865a2b`)
- **tests** — the sending probe, the bank across meetings and the whole chain (`aa5f9e8`)
- **tests** — the voice, the hard cases, the models and the layers, in English (`13e9bf4`)
- **tests** — the style, the SMTP session and the updates, in English (`bc7f494`)
- **tests** — the last comment of the recording tests (`e17cbb1`)
- **tests** — the names and the recording, in English (`b5e8012`)
- **tests** — the questions and the meeting in a room, in English (`ef9712c`)
- **tests** — the installer and the voice bank, in English (`53f8724`)
- **tests** — the integration tests' prose, in English (`81889ce`)
- **tests** — the manners and the settings, in English (`66628b4`)
- **tests** — the window and the conversation harness, in English (`857c1f6`)
- **tests** — the channels and the audio hardware, in English (`9fee304`)
- **tests** — the published thread's prose, in English (`dc31afe`)
- **tests** — the live thread's prose, in English (`10e50ee`)
- **src** — the last French prose outside what the software shows (`5e590f7`)
- **tests** — the assistant's prose, in English (`87798d2`)
- **tests** — the voiceprint matching prose, in English (`af3815c`)
- **tests** — the processing chain's prose, in English (`39a6bbc`)
- name the diagrams in English, and show the one nobody could see (`5e32469`)
- the window, the command line and the wiring, in English (`f9dac20`)
- **adapters** — the tools, in English (`0626132`)
- **adapters** — the nine biggest adapters, in English (`f0b9c8a`)
- **application** — the remaining use cases, in English (`ee8a96f`)
- say the ports and the application in English (`367ca21`)
- **domain** — say the domain in English, in one line each (`569f1cf`)
- **rex** — what the meeting of 2026-09-10 actually measured (`9f3434c`)
- ouvrir l'étude de la séparation des voix, et le banc qui la tranchera (`70dba54`)
- ouvrir le retour d'expérience de la réunion du 10, avec ses chiffres (`f4696fc`)
- record what the first morning of use showed, and what it left open (`7728f4b`)
- **publier** — sortir le workflow du chemin que le jeton ne peut pas écrire (`1fced44`)
- **skill** — teach the repair skill what this week added (`808596c`)
- **readme** — say that it takes part, and how to keep it quiet (`a5ba437`)
- record the night the tool learned to take part (`ae73bff`)
- **sauvegarde** — a synchronised folder sends the data to its host (`a7fab7d`)
- **calibrage** — four series say the threshold is right, one series said wrong (`0b0191b`)
- **skills** — write down how to assist a meeting while it runs (`8480b47`)
- name the machine's guard by its role, not by its product (`6611bf4`)
- write the engineering log and the calibration notes in English (`fc21a2d`)
- **readme** — describe the tree that exists, and say what enforces it (`81f66f7`)
- **assets** — redraw the two diagrams in English, and fix what they hid (`a6899e9`)
- **readme** — write the README in English (`5fc9696`)
- measure the chain against a real four-person meeting (`9d85063`)
- **readme** — stop claiming the chain runs on a Mac's graphics card (`5dbf48b`)
- record what real recordings showed, and what the run costs (`269e1e5`)
- record the first run on a real Linux desktop (`dc392ef`)

## Refactored

- **cuda** — gather the loading of the nvidia libraries in one adapter (`cb73bd2`)
- **layers** — two primary adapters were leaning on each other (`5cfe84d`)
- **install** — the code, in English; the messages, in French (`e0a5314`)
- **domain** — the edit distance comes from a library (`e45cf35`)
- the French words the rename had left in every layer (`b0b57b3`)
- **domain** — the thresholds, the live thread and the questions (`f251a95`)
- **domain** — English identifiers in the domain layer (`d555b0d`)
- the tools directory and its files, in English (`630efca`)
- name the remaining classes in English (`d7660f5`)
- drop the comment blocks, and say the domain in English (`cef540a`)
- **tests** — name the test files after the modules they exercise (`7739a54`)
- the code speaks English (`166f37a`)
- **config** — pin the file keys with validation aliases (`ead2a3c`)
- drop the field comment blocks (`f7bd18e`)
- **traiter** — name the report title import for what it is (`d1358ea`)
- **couches** — put every module in the layer its imports say it belongs to (`e0653e9`)

